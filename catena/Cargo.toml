[package]
name = "catena"
version = "0.1.0"
edition = "2024"

[dependencies]
hecs = "0.10"
serde = { version = "1", features = ["derive"] }
tokio = { version = "1", features = ["rt-multi-thread", "macros"] }

[dev-dependencies]
kani-verifier = "0.58"
